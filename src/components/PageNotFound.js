import React from 'react';

const PageNotFound = () => (
    <div className="page-not-found">
        <h1>Page not found, please click on above navigation options to redirect to login page </h1>
    </div>
);

export default PageNotFound;
